# 教材動畫製作規格

動畫的目標是讓讀者把數學物件和物理量連起來。第一份實作是 [單關節動畫模組](00_single_joint/src/animation.py)，由 [教學原稿](00_single_joint/build_lesson.py) 呼叫並產生 notebook、HTML 與靜態圖。

## 🎯 每個動畫先回答一個問題

| 單元 | 要看懂的問題 | 可視化內容 | 狀態 |
|---|---|---|---|
| 單關節 | 瞬間靜止為什麼仍需慣性項？ | 姿態、公式項數值、時間游標 | 已實作 |
| Lagrange 推導 | 偏微分後為什麼還要取時間導數？ | 固定變數、作用中的算子與結果依序顯示 | 預留 |
| 雙連桿 | 一個關節加速如何影響另一個關節？ | 肢段與 $M_{ij}\ddot q_j$ 各項同步 | 預留 |
| 三維運動學 | Jacobian 如何把座標速度映到空間速度？ | 座標軸、微小位移與 Jacobian 欄向量 | 預留 |
| 肌肉冗餘 | 為什麼不同肌肉力能得到相同力矩？ | 可行線段、等高線、權重與能力上限 | 第一课已實作；零空間向量動畫待加入 |
| 直接配點 | 離散軌跡如何滿足動態方程？ | 節點、插值與動態缺陷值 | 預留 |

## 🧩 數值與畫面分開

物理函式先產生時間、狀態與推導量；繪圖函式只消費這些數據。現有 `make_animation(model, times, q, velocity, acceleration)` 接受 SI 單位陣列，不在動畫回呼內積分。未來多自由度資料可用 `(n_coordinates, n_times)` 的狀態陣列，但須為新函式明確記錄維度，不能默默更改現有介面。

公式動畫的每一個階段應記錄：原式、當前操作、固定變數、結果、物理意義與靜態替代圖。若未來加入 Manim 或其他渲染器，沿用同一份符號表與數值輸出；目前不要求這些額外依賴。

## 🎞️ 播放與閱讀要求

- 提供播放、暫停、逐格與時間定位；預設不自動播放。
- 文字標示單位、正方向，以及畫面是指定運動、前向模擬或最佳化結果。
- 色彩輔助對應公式，但每项都要有文字標籤；相同物理量跨畫格維持同色。
- 同一動畫的座標軸與色條尺度固定；示意箭頭不按大小繪製時必須標明。
- 在運動轉折處提供靜態畫格與文字解釋，確保不執行 JavaScript 也能學習。
- 角度在顯示層才轉成度；物理計算維持 rad。

## ✅ 製作與驗收

先驗證物理模型，再匯出动画；檢查首格、中間轉折與末格的符號、單位、數值及版面。Notebook 的 HTML 輸出會被 Jupyter Book 沿用；實際可否播放仍取決於檢視器的 JavaScript 政策。

目前的動畫是數值與運動同步，尚未製作代數式逐項變形動畫。後者應先完成逐步推導脚本，再決定畫面轉場，避免跳步只是被動畫掩蓋。
